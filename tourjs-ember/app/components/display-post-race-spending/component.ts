import Component from '@ember/component';

export default class DisplayPostRaceSpending extends Component.extend({
  // anything which *must* be merged to prototype here
}) {
  // normal class body definition here
};
