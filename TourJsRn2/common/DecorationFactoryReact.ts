import { DecorationFactory, ThemeConfig } from "./DecorationFactory";
import {GImage} from '@flyskywhy/react-native-gcanvas';

export class ReactDecorationFactory extends DecorationFactory<GImage, any> {
  constructor(themeConfig:ThemeConfig) {
    super();


  }
}